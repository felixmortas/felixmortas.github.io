{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve -p 3000 .",
    "build": "mkdir -p dist && cp index.html dist/",
    "preview": "npx serve -p 3000 dist",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {}
}
